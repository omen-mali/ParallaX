# Required only for the selected live provider. Never commit real keys.
OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional provider and model overrides. Mock is the safe default.
# PARALLAX_PROVIDER=mock
# PARALLAX_MODEL=
